# Orchestration scratch (plans/state are not committed)
/.orchestration

# Rust build artifacts
/target
**/target/
**/*.rs.bk

# IDE
.vscode/
.idea/
*.iml
*.swp
*.swo

# OS
.DS_Store
Thumbs.db
desktop.ini

# Environment / secrets
.env
.env.local
*.pem
*.key

# Coverage / profiling
*.profraw
/coverage/
/coverage.json*
/coverage.txt*
/coverage.*.stderr.log
/coverage.stderr.log
/verify.stderr.log
/verify.*.stderr.log

# Cargo.lock is intentionally tracked (workspace contains a binary).

# stray path-mangled redirect artifacts
FProjects*

# stray placeholder-path artifact (LLM <memory> leak, NTFS-remapped brackets)
crates/spt-tui/memory
docs-book/book/

# CycloneDX SBOM output (generated in CI, uploaded as artifact)
*.cdx.json
*orchestrationlogs*
