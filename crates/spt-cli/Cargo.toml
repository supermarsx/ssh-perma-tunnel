[package]
name = "spt-cli"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
authors.workspace = true
description = "Clap-derived command-line interface for spt."

[features]
default = []
snmp = []
testing = []

[dependencies]
clap = { workspace = true }
clap_complete = { workspace = true }
# `humantime::Duration` is used as the value type for `--timeout` on the
# `spt kill` subcommand (and is the canonical duration parser elsewhere in
# the workspace).
humantime = { workspace = true }
spt-core = { workspace = true }

[dev-dependencies]
insta = { workspace = true }

[lints]
workspace = true
